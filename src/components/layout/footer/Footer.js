import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  border: 1px solid black;

  background-color: #fff;
`;

export const Footer = () => {
  return <Wrapper>Footer component</Wrapper>;
};
