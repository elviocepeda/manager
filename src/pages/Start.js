import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  display: flex;
  align-items: center;
`;

export const Start = () => {
  return (
    <Wrapper>
        Start
    </Wrapper>
  );
};
